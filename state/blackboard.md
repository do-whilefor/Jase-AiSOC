# Security validation blackboard

```yaml
tested:
  - target: repository maintainer × plan Appendix A × Rust/Linux workspace directory skeleton × phase-boundary preservation
    finding_status: lead
    rating: unrated
    evidence: all 44 directories named or implied by the Appendix A tree are present, including 24 Rust crate directories, .cargo, schemas/proto/rules/migrations/fixtures/replay/benches/fuzz, six deploy subdirectories, ADR/threat-model/operations/compatibility docs, and tools; 42 otherwise-empty directories carry .gitkeep; repository-layout.md maps each crate to its plan responsibility and first main phase; no future crate contains Cargo.toml and the root Workspace still contains only crates/aisoc-contracts, so directory reservation is not represented as P1-P14 implementation; no Cargo or project command was executed
    next: keep the placeholders inert while P0 remains open; after P0 Linux gates pass, begin P1 by replacing only the P1 placeholders with the plan-mandated toolchain/governance and foundational Rust crates

  - target: unauthenticated/version-skewed subject × Agent/Web/Control/Evidence context × root/nested/tagged schema boundary × missing/unsupported version/unknown field
    finding_status: lead
    rating: unrated
    evidence: AuthenticatedAgentContext, WebIngressContext, AuthenticatedRequestContext, and EvidenceAccessContext all carry SchemaVersion; their boundary guards reject a context other than 1.0.0; all cross-boundary _id/_ids fields, including registry/external IDs, are typed newtypes; WafRuleId runtime deserialization and Schema both allow only safe nonempty slash-separated registry segments and reject URL/colon/empty/traversal-shaped selectors; all 47 fixed structs/tagged object enums statically carry deny_unknown_fields; the existing Schema test requires the 23-item manifest and generator output to each have unique filenames and equal sets, shares a versioned safe lowercase ASCII basename predicate with the exporter and directly rejects traversal/path/uppercase/bad-boundary/wrong-major/dotted names, and recursively requires additionalProperties=false for every generated node with nonempty fixed properties while leaving guarded dynamic-key maps open; the exporter runs manifest/generator/filename preflight before directory creation or file access; a direct unexecuted Audit sample rejects a forged nested correlation field; source exists but was not executed
    next: on Linux, generate all 23 Schema documents; execute manifest/generator uniqueness, safe filename, recursive fixed-object strictness and drift=0; then run negative deserialization/contract cases for missing, malformed, old, and future versions plus unknown fields at root, nested DTO, and tagged variant boundaries and retain request/decision evidence

  - target: authenticated identity × Agent/Web/Control entry points × tenant-scoped objects × ownership binding
    finding_status: lead
    rating: unrated
    evidence: crates/aisoc-contracts/src/agent.rs, web.rs, and control.rs encode server-authenticated tenant/agent/host/service binding; Web ingress context and request schema versions plus tenant/service/route bindings now cover every WebBindingDecision outcome directly; Control authentication is versioned, requires exactly one actor, binds OIDC to User and mTLS to ServiceIdentity, and accepts only bounded unique token-shaped roles/attributes; Control then requires the authenticated tenant, client object declaration, and a separately constructed AuthoritativeObjectScope from the server repository/registry to agree on both object and owner tenant; all AuthenticationContextDecision and TenantScopeDecision outcomes have direct source cases, but none was executed
    next: on Linux, execute Web/SOC/Control contract tests with two tenants, human/service identities, auth-kind substitution, client object substitution, authoritative owner/object substitution, missing/duplicate claims, and replayed envelopes; then bind AuthoritativeObjectScope construction to repository lookups

  - target: untrusted Web request/model subject × WebSecurityEvent × security state/policy result × attempted-versus-successful outcome
    finding_status: lead
    rating: unrated
    evidence: validate_web_request_contract rejects authority userinfo/delimiters, URI control/whitespace/fragment/backslash ambiguity, invalid or oversized parser/authority/URI/selected fields, a nonempty body without its digest, parsed body fields on a zero-length body, parsed body fields without Content-Type, and sensitive header/query/body names; Content-Type is bounded ASCII type/subtype with unique token/quoted-string parameters, rejects control characters and malformed quotes, and requires one multipart boundary whose decoded value is 1-70 RFC 2046 bchars bytes and does not end in a space; direct positive samples retain a quoted semicolon plus token and quoted-colon boundaries; validate_web_security_event rejects suspected_success and confirmed_compromise, prevents monitor/shadow from reporting execution decisions, binds BLOCK exactly to blocked state, requires same-tenant contract-valid unique nonempty bounded Evidence collected no later than the decision for attack/blocked results, rejects invalid or duplicate Rule/reason data, and requires fully exclusive deterministic/model/route-fail-policy provenance; WebDataMinimizationDecision, all 24 WebRequestContractDecision outcomes, and WebSecurityEventDecision have direct source cases, but sources were not executed
    next: on Linux, execute authority/URI/framing differential and bind raw Content-Type/Content-Length/Transfer-Encoding/body-boundary observations to the DTO; recompute raw/canonical digests and exercise Web state/policy/source combinations, then verify that only downstream Detection/Incident can elevate success state using process/file/network/authentication Evidence

  - target: optional Web AI provider × tenant/service/route fail policy × timeout/budget/circuit/unavailable/invalid-output state × fallback decision
    finding_status: lead
    rating: unrated
    evidence: WebRouteFailPolicy is a versioned Rust contract keyed by typed tenant/service/route/Policy IDs with explicit dispositions for every frozen AI failure kind; validate_web_fail_policy_application requires an accepted WebSecurityEvent whose route-fail-policy source, tenant, service, route, policy ID/version, failure kind, and decision match the separately loaded authoritative policy; invalid structured output must identify the failed model run while pre-run failure kinds cannot claim one; ModelAssessment now has an exclusive typed Web Request or Incident subject, and validate_web_model_assessment_binding first binds the request to server-owned WebIngressContext, then requires accepted request/event/assessment contracts plus exact tenant/request/model-run binding, service/route consistency, at least one Event-contained Evidence reference collected by assessment completion, no Incident Claim, and request/evidence <= assessment <= decision; WebRouteFailPolicyDecision, WebFailPolicyApplicationDecision, and WebModelAssessmentBindingDecision now have direct cases for every outcome, but were not executed
    next: on Linux, inject Provider timeout, circuit-open, budget-exhausted, unavailable, and structurally invalid output for multiple routes; replay a valid assessment across another tenant/request/service/route and prove rejection; then prove deterministic Web/SOC processing continues with only the configured route disposition

  - target: authenticated Agent × AgentEnvelope payload × nested SecurityEvent/EvidenceRef × tenant/host/agent/boot/sequence/contract variables
    finding_status: lead
    rating: unrated
    evidence: validate_agent_binding checks context/envelope schema versions and authoritative mTLS tenant/agent/host before nested content; each Event must match tenant/host/agent, declare the envelope boot and an in-range sequence, preserve strict batch ordering and exact first/last boundaries while keeping gaps observable, pass SecurityEvent/EvidenceRef subcontracts, and match a recomputed SHA-256 over project canonical JSON with recursively sorted object keys; all 24 AgentBindingDecision outcomes now have direct positive or negative source cases, including empty/oversized payload and digest mismatch; all 21 SecurityEventDecision outcomes now have direct cases covering Schema, Evidence tenant/subcontract/coarse producer-source lineage, entity identity/capacity, category/action/source/version, Agent source identity, Linux process/network/file/auth, labels, and extensions, plus a positive matrix for Agent, Web Guard, Suricata/Falco, auditd/journald/procfs/netlink/service log, file scan, Response Runner, and Import producer mappings; a stale nonexistent EventSourceKind::Sensor sample was corrected to the plan-backed Journald source while retaining the missing authoritative Agent ID case; offline created_at/authenticated_at ordering, compression ratio, receive-time drift, batch persistence, and certificate authority were explicitly left to their P2/P3 owners; sources exist but no test was executed
    next: on Linux, execute the complete Agent binding and Event/Raw Evidence source-lineage matrix with real mTLS identities and certificate revocation, then validate compressed/raw limits, receive-time drift, batch replay, gap recovery, same-key/different-digest conflict, and disconnect resume against Ingest persistence

  - target: Incident service × Incident revision/timeline × authoritative Detection/Claim/Evidence/Entity relationships × revision/time/reference/confirmation variables
    finding_status: lead
    rating: unrated
    evidence: validate_detection_contract now has direct cases for all 17 outcomes covering Schema, Evidence tenant/subcontract/duplication/required lineage, confirmed size/integrity/custody, observation window/count, Rule metadata, entity limits/identity, and suppression state; validate_incident_contract enforces revision links, RFC3339 business-time and timeline ordering, same-tenant valid non-future EvidenceRefs, timeline-to-Evidence closure, and nonzero verified non-expired Evidence plus Assurance for confirmed compromise; validate_incident_relationships requires exact server-resolved Detection/Claim sets and a separately resolved custody-chain set capped at 512, rejects duplicate chain evidence IDs, chains outside the Incident revision, and every malformed chain even when no current Claim references it, then requires accepted child contracts, same tenant/incident, no future Detection/Claim, Detection Evidence and Entity closure, typed Detection Host membership in the revision EntitySet, Claim Evidence closure, linked Detection origin, an access-context Evidence set contained by the revision, and for confirmed incidents access to every revision Evidence with a complete accepted custody chain plus either a confirmed Detection or an independently verified Claim requesting confirmed compromise; the Host guard prevents a substituted typed host from being hidden behind an unchanged stable-key string without predefining the P4 association algorithm; same_evidence_identity allows integrity/custody lifecycle advancement while rejecting locator/hash/size/source/classification/collection-time substitution under the same Evidence ID; validate_incident_revision_transition binds adjacent revisions and tenant/incident/created/revised time, preserves Detection/Evidence/Claim relationships, rejects lifecycle regression, and retains prior Timeline facts as a multiset while allowing late events to be inserted by occurred_at; all 34 IncidentRelationshipDecision outcomes have direct source cases, but none was executed
    next: on Linux, execute the Incident relationship and adjacent-revision matrices with repository-resolved objects and complete custody chains, Detection Host/Entity substitution, scoped and complete Evidence access, late events, same-ID metadata/chain substitution, duplicate Timeline facts, RFC3339 offset-equivalent times, and concurrent revision attempts

  - target: AI/Incident subject × EvidenceRef/custody-chain relationship × confirmed-compromise claim × existence/tenant/integrity/time/sequence/hash variables
    finding_status: lead
    rating: unrated
    evidence: crates/aisoc-contracts/src/ai.rs, incident.rs, and evidence.rs reject URL/path-shaped store selectors plus missing, duplicate, invalid, zero-byte, cross-tenant, unauthorized, over-classified, failed-integrity, expired-custody, or future-collected evidence; CustodyRecord binds schema/tenant/Evidence digest/nonzero sequence/state/time/typed actor/operation/source version/previous-record hash into record_hash; adjacent transitions bind tenant/Evidence/digest, exact next sequence, exact previous hash, monotonic custody, and the frozen integrity state machine; EvidenceCustodyChain binds the collection record and latest EvidenceRef state, caps records at 4096, and uses sequence rather than a rewindable business clock as ordering authority; EvidenceAccessContext and caller-provided custody-chain sets are each unique and capped at 512, foreign/duplicate/oversized/missing/tampered chains fail closed, and direct source cases cover all 10 CustodyRecordDecision, 10 CustodyTransitionDecision, 11 EvidenceCustodyChainDecision, 12 EvidenceUseDecision, and 24 ClaimVerificationDecision outcomes; Incident revisions reject Evidence collected after revised_at; validate_evidence_package_binding requires the authoritative tenant/incident/revision and exact EvidenceRef equality, preventing same-ID locator/hash/classification/integrity/custody substitution; EvidencePackage rejects evidence collected after package creation and Claim verification rejects evidence collected after Claim creation, while equal instants remain valid; all Claim statuses are bound to the access-context tenant and incident, and any referenced Evidence plus its complete server-resolved chain is validated before contradicted/unsupported/human-review decisions are returned; ModelAssessment uses an exclusive Web Request or Incident subject; validate_model_assessment_binding requires the Incident subject, accepted package/claim subcontracts, one tenant/incident/model run, an exact returned Claim set, assessment Evidence drawn only from the package, Claim Evidence declared by the assessment, and package-created <= claim-created <= assessment-completed; verifier ID/version must be paired and readonly-tool self-verification is rejected even while proposed; a proposed Claim with valid Evidence returns EvidenceValidated rather than Verified; ModelAssessment records typed Provider/Model/Prompt identities and their versions; EvidencePackage is capped at 512 refs and 64 MiB; source tests are written but not executed
    next: on Linux, execute the Evidence authorization and Claim/review negative contract matrices with repository-backed complete-chain resolution, atomically append custody records with compare-and-advance, verify actual object digests, exercise concurrent/gap/reorder/truncation/tamper and actor substitution, Web/Incident subject/cross-review substitutions, RFC3339 offset-equivalent/equal/future timestamps, classification, and complete evidence coverage, then retain original request/decision evidence

  - target: Policy/Approval subject × ResponseAction/Incident revision/Evidence graph × R2/R3 state × replay/target-change/approval variables
    finding_status: lead
    rating: unrated
    evidence: crates/aisoc-contracts/src/response.rs defines typed action-to-tier-to-capability-to-target mappings, Linux-safe immutable target snapshots, nonzero Incident revision bound into the canonical digest, a 24-hour validity ceiling, unique approval IDs and approver identities, 16-attestation and 512-Evidence ceilings, nonempty unique Supporting Evidence for R2/R3, digest-bound time-windowed attestations, explicit rejection fail-closed, critical-asset dual approval, R2 TTL/window/deadline/registered-inverse binding, R3 opaque human recovery runbook, and idempotency; separately constructed ResponseAuthorizationContext records server-resolved tenant/action/Incident revision/Policy/digest/full approval attestations, and validate_response_authorization_binding requires exact authority agreement, authorization after all approvals and within action validity, an accepted matching authoritative Incident revision no later than request time, an access context limited to that revision, a custody-chain set capped at 512 with unique Incident-member Evidence IDs and every supplied chain valid even when not selected as Supporting Evidence, and every Supporting Evidence to have a complete accepted chain and be a pre-request, authorized, nonempty, integrity-verified, non-expired member; all 36 ResponseContractDecision and 29 ResponseAuthorizationBindingDecision outcomes have direct source cases, but none was executed
    next: on Linux, execute the complete response contract and authority-binding matrices with repository-backed Policy/Approval/Incident/Evidence/custody, authenticated queue identities, cross-tenant/action/revision/digest/approval/chain substitutions, and Evidence classification/integrity/custody/time variants; then validate runner capability authorization, live PID/inode/hash/agent fingerprint target revalidation, approval replay/expiry, rollback, post-check, and idempotency against real runners

  - target: platform service × Audit/Error boundary × typed primary object/correlation/safe attributes/hash chain × stable-ID substitution/secret-name/cardinality/value-length/content/stream/sequence/previous-link variables
    finding_status: lead
    rating: unrated
    evidence: SchemaVersionDecision 2/2, SafeFieldsDecision 8/8, AuditContractDecision 16/16, AuditChainTransitionDecision 8/8, and ErrorContractDecision 6/6 now have direct source cases; validate_safe_fields rejects password, authorization, cookie, key, secret, and token names plus empty/non-token/oversized keys, control-character or oversized values, and oversized collections; AuditEvent carries typed audit_stream_id and nonzero sequence, binds both into event_hash, and supports Request, Host, Agent, Service, and Route as typed primary objects; AuditCorrelation freezes optional typed request/host/agent/service/route fields alongside data/governance object IDs, and any supplied same-type correlation must match the primary object while tenant remains the single top-level authority; AuditEvent rejects invalid first/later link shape, missing/ambiguous actor, empty/oversized/invalid code fields, stable-resource and governance correlation substitution, tenant substitution, unsafe attributes, and hash tampering; validate_audit_chain_transition rejects invalid endpoints, cross-stream or cross-tenant splicing, duplicate Event ID, gaps, and wrong previous Hash while deliberately not treating a rewindable business clock as chain-order authority; ErrorEnvelope rejects unsupported schema, empty/noncanonical messages, invalid retryability, and unsafe context; source exists but no test, log, or response was executed or generated
    next: on Linux, execute boundary, retryability, Request/Host/Agent/Service/Route object-correlation substitution, and audit stream/sequence/tamper cases; then validate append-only journal atomic compare-and-advance, continuity/signature anchors/concurrency, and inspect journald, HTTP responses, traces, metrics labels, and browser assets for real secret propagation
```

No vulnerability status or rating is assigned: this turn performed code authoring and static review only, with no dynamic requests, responses, runtime state, or Linux execution evidence.
