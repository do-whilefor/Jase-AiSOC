```yaml
tested:
  - target: tenant credential × /api/v1/hosts × cross-tenant host ownership × read × bearer/header variation
    finding_status: closed
    rating: unrated
    evidence: "Post-fix PostgreSQL integration and deployed-container requests: tenant A credential reading tenant B host returned 404; adding X-Tenant-ID for tenant B returned 403; tests/integration/test_p1_api.py reproduces both comparisons."
    next: "Reopen if another authentication mode, batch endpoint, or object API derives tenant_id from caller-controlled headers/body instead of the authenticated principal."
  - target: agent identity × local AgentEnvelope/queue × event tenant/host/boot/sequence × buffer/batch × body variation
    finding_status: closed
    rating: unrated
    evidence: "AgentEnvelope validation rejects mismatched tenant, agent, host, boot, and sequence before enqueue; the SQLite file is bound to one configured tenant/agent/host identity; tests/unit/test_agent_contracts.py and test_agent_queue.py reproduce the negative comparisons."
    next: "Reopen at P2 mTLS/P3 ingest when certificate subject-to-agent/tenant binding and server-side body revalidation are implemented dynamically."
  - target: agent queue × P0/P1/P2/P3 event priority × capacity/retry/corruption × state transition × queue pressure
    finding_status: closed
    rating: unrated
    evidence: "Capacity tests show P0 enters protection mode with dropped.p0=0; P2 replaces auditable P3 data; partial ACK and network release reuse the immutable batch ID/digest; corrupt protected data is quarantined and raises protection. Linux container smoke ran as UID 10001 with database mode 0600 and recovered/ACKed two events after restart."
    next: "Reopen for real disk-full/WAL fault injection, process kill during fsync, and receiver-side ACK/DLQ integration."
  - target: tenant/anonymous Agent × registration API × tenant Host/CSR × enroll/replay × tenant, token, CSR identity variation
    finding_status: closed
    rating: unrated
    evidence: "tests/integration/test_agent_identity.py: tenant B credential requesting tenant A Host token returned 404; the database contained only the token digest; an invalid CSR returned 401 without consumption; a valid P-256 CSR with forged Subject/SAN enrolled using server-side identity; replay returned 401."
    next: "Reopen when registration is exposed through a TLS proxy or another Host creation/batch enrollment path; confirm proxy routing cannot alter signer or tenant context."
  - target: registered Agent × certificate lifecycle service × Agent identity/certificate × rotate/revoke/re-enroll × proof and certificate state
    finding_status: closed
    rating: unrated
    evidence: "Real ssl sockets required a CA-signed client certificate. PostgreSQL integration rejected rotation signed by the new/wrong key, accepted proof from the old private key, rejected the old certificate after rotation, accepted the new certificate, rejected it after tenant revocation, and accepted a newly authorized installation only after explicit re-enrollment."
    next: "Reopen at Ingest integration and validate peer-certificate extraction from the actual TLS server; never accept a caller-supplied certificate header without a trusted proxy contract."
  - target: copied Agent image × local identity/session service × installation and certificate × concurrent use × machine binding and lease state
    finding_status: lead
    rating: unrated
    evidence: "Local store comparison raised clone detected when the current machine/DMI digest differed. Two concurrent PostgreSQL transactions using the same valid certificate produced one lease and one active_identity_lease conflict. A real mTLS Ingest gateway now exists (src/blue_team/ingest_gateway, src/blue_team/agent_core/transport.py): every heartbeat/events connection terminates mutual TLS, reads the client certificate, calls renew_agent_session (storage/agent_identity.py) to renew-or-acquire the single-active lease, and revalidates batch tenant/agent/host against the certificate identity. Verified 2026-08-04 against a real PostgreSQL 17 container (Docker Desktop Linux Engine) via tests/integration/test_ingest_mtls.py: enroll→heartbeat→batch→ACK produced 2 agent_events + 1 agent_heartbeat + 1 agent_session + 2 immutable evidence objects, accepted_sequence=sequence_end, and a second connection presenting the same cert with no session value was rejected 409 while the lease was active. tests/unit/test_transport.py covers the mTLS client (ssl context, X-Agent-Session header, ack/release, status rejection)."
    next: "The lease is enforced per authenticated connection at the gateway and tested at the aiohttp/httpx level; tests/integration/test_ingest_mtls.py and tests/smoke/linux_ingest_e2e_smoke.py both reproduce enroll→heartbeat→batch→ACK→clone-reject-409 under a real PostgreSQL (Docker Linux Engine) and are green (2026-08-04). Remaining open item is a real VM disk-copy clone where the original and clone cannot remain simultaneously active across a long window, plus the real Linux VM matrix (eBPF/auditd/DEB/RPM/systemd). Software binding still cannot resist root plus fully replicated hardware identifiers and the leased session value; evaluate TPM-backed attestation separately."
  - target: Agent runtime × Collector/heartbeat/SQLite queue × collection admission and health × lifecycle/pressure/failure × state transition
    finding_status: closed
    rating: unrated
    evidence: "test_agent_runtime.py covers deterministic runtime states and a real SQLite P0 protection transition. test_agent_process.py and test_agent_supervisor.py add a real blue-team-agent entry, private bounded config, single-instance state lock, fsync lifecycle/Heartbeat journals, ordered started/healthy protocol, separate startup/health/TERM/KILL deadlines, combined output budget, literal argv, sanitized environment, closed FDs, and process-group cleanup. Linux UID 10001 ran 43 relevant pytest cases plus tests/smoke/linux_agent_supervisor_smoke.py against image sha256:52287cf75a644a8670be48e70765636dacf487ab2297737868945c148fe82e6c: real CLI stopped cleanly, a competing instance was rejected, a TERM-ignoring probe was killed/reaped, hardlink substitution was rejected, and state files remained 0600."
    next: "Reopen when Collector drivers become subprocesses or Heartbeat uses mTLS network transport; fault blocked drivers, transport timeout/replay, journal disk-full/rotation, supervisor restart, and kill during each persisted lifecycle transition."
  - target: release signer × Agent verifier/installer/state store × artifact tar/content/target/version × verify/stage/health/activate/rollback/recover × key, kind, path, content, sequence, approval, lock, and revision variation
    finding_status: closed
    rating: unrated
    evidence: "test_agent_releases.py and test_agent_installer.py verify signed persistence plus health-gated tar install/upgrade, including signature/target/replay/floor/approval/path/symlink/hardlink/size/content/journal/lock variations. InstalledReleaseProcessHealthCheck resolves only the manifest-declared executable from the uncommitted candidate and invokes fixed health-probe argv through the bounded supervisor. Linux UID 10001 dynamically installed a wrapper around the real Agent CLI, committed only after ordered startup/health and graceful stop, then rejected a health-timeout upgrade, deleted its candidate, and preserved the old active/state and queue sentinel. Current PostgreSQL Windows gate: 121 passed/8 platform cases skipped; Linux relevant gate: 43 passed plus standalone smoke."
    next: "Reopen at downloader/systemd/policy integration. Test interrupted or substituted downloads, kill -9 and power loss at each fsync/rename boundary, privileged updater versus unprivileged Agent ownership, bypass writers outside the installer lock, key rotation/revocation, fleet rollout stop conditions, and real DEB/RPM/tar install/upgrade/rollback/uninstall on the fixed Linux VM matrix."
  - target: local application/test subject × Settings constructor/get_settings × Agent CA configuration × bootstrap × .env presence variation
    finding_status: closed
    rating: unrated
    evidence: "A repository .env containing Kali CA paths caused 4/198 local tests to fail and made the CA-pair negative test pass incorrectly. Direct Settings construction is now dotenv-independent; only get_settings reads .env. tests/unit/test_config.py and test_api_basics.py reproduce both paths; after the third P5 increment the full local gate is 255 passed/15 platform, symlink, or PostgreSQL cases skipped."
    next: "Reopen if another library/test entry point calls get_settings implicitly or a process environment value can silently override an explicitly supplied security setting."
  - target: detection worker × PostgreSQL detections × host/source entity × persist/replay × identical tenant/rule/window variation
    finding_status: technical_hit
    rating: unrated
    evidence: "Migration 0006 and create_detection deduped only tenant+rule+window, so two hosts or source entities sharing a window collapsed. Migration 0007 added host_id+entity_key. A second review found rule_version was still absent, so a newly governed rule version could be swallowed by the same old-version subject/window. Migration 0016 and both normal/race repository predicates now include rule_version; the integration test expects one replay plus independent host, entity, and version rows. Offline PostgreSQL upgrade/downgrade SQL reaches 0016, but no live database ran."
    next: "Run alembic upgrade/downgrade and tests/integration/test_detection_persistence.py on Kali PostgreSQL; verify 1 replay row plus three independent host/entity/version rows, exact governance references, and audit counts before closing."
  - target: normalize worker × partition watermark/normalized event × late immutable fact × ingest/replay × new versus duplicate dedupe key
    finding_status: technical_hit
    rating: unrated
    evidence: "The prior worker never loaded/advanced watermarks, while the repository's claimed late revision conflicted with tenant+dedupe and tenant+event_id unique constraints. Worker tests now dynamically classify old events, advance watermarks, retain object-read failures in DLQ, and prove a duplicate late key returns the original fact. PostgreSQL out-of-order replay was not run this turn."
    next: "On Kali PostgreSQL ingest ordered 1,3,2 plus a duplicate 2; verify one immutable row per unique event, late_arrival only outside allowed lateness, monotonic watermark, and no duplicate DLQ/detection."
  - target: Suricata/journald subject × SSH normalization × authentication outcome × classify × protocol-only versus explicit sshd identity/message
    finding_status: closed
    rating: unrated
    evidence: "Suricata ssh.open previously became success without authentication evidence. It now remains unknown unless an explicit failure signature exists. Parameterized journald tests map identity-verified sshd Failed/Accepted messages, and an app.service message with identical text remains service_log.line. The regenerated ssh replay uses 11 real-shaped sshd failures across 3 users and produces one attack_attempt."
    next: "Reopen for PAM/auditd variants, locale/version message changes, duplicate journald/audit records, Accepted-after-fail correlation, or any rule that treats protocol metadata as login success."
  - target: Agent audit Collector × serial-group contract/normalizer × syscall records × aggregate/normalize × boot, serial, type, EOE, escaping, capacity variation
    finding_status: closed
    rating: unrated
    evidence: "AuditdSerialAggregator unit comparisons keep interleaved serials separate, complete only at terminal EOE, and emit capacity/record-bound/shutdown groups as incomplete evidence. AuditdNormalizer tests reject incomplete, boot-mismatched, serial-mismatched and type-mismatched groups; valid EXECVE/SYSCALL/PATH/CWD/PROCTITLE/SOCKADDR/USER_AUTH groups preserve argv, paths, PAM identity, destination and a stable host+boot+serial dedupe key while the same boot+serial on another host remains distinct. These are offline contract results, not a real auditd Collector claim."
    next: "Reopen on Kali/Ubuntu/Rocky with native audit output; compare EOE/timeouts, backlog/drop, quoted/hex fields, arch-specific syscall numbers, IPv6/unix sockaddr and restart/boot transitions against uploaded raw evidence."
  - target: detection worker × P5 process/file/network sequence × process generation/path × correlate/persist × boot, PID reuse, missing source, order, duplicate, time skew variation
    finding_status: technical_hit
    rating: unrated
    evidence: "The prior 120-second detection lookback could not contain the new 300-second P5 chain window; after that fix, DetectionWorker still read ascending events with a global limit=2000, so high-EPS windows could silently omit newer chain stages. Settings now require a 600-second lookback, entity keys are bounded, the worker reads newest-first with max_events+1 and raises DetectionWorkerBatchOverflow instead of evaluating a partial window. A restart comparison first sees only downloader/write (0) and then reconstructs the complete four-stage immutable DB window (1 download-execute detection). Five hashed P5 replays produce exactly 5 expected success detections and 0 for normal/failed/missing/skew sets. No PostgreSQL worker cycle or real Collector ran this turn."
    next: "On Kali PostgreSQL feed chains spanning multiple polls/restarts plus same-PID reuse, duplicate and late facts; verify independent detections persist once, then exceed 20000 events and prove the cycle emits an observable overflow without partial detections."
  - target: Agent process × audit.log polling Collector/local queue × canonical event enqueue × run/restart/concurrency × registration, sequence, capability-state variation
    finding_status: technical_hit
    rating: unrated
    evidence: "Code-path review showed blue-team-agent run registered no real Collector and LocalDiskQueue had no persistent sequence allocator, so enabling more than one producer or restarting a producer could reuse boot+sequence. AgentRuntime now explicitly polls registered drivers, propagates degraded/failed capabilities and preserves last drop/backlog/parse/incomplete counters. AgentProcess registers AuditdCollector; SQLite tests show concurrent durable allocation, manual sequence floor advancement and legacy ACK-audit recovery without reuse. Audit collector tests enqueue complete serials as P2 and gaps as P1. These are Windows temporary-file/SQLite results, not native auditd evidence."
    next: "On Kali run the actual Agent against /var/log/audit/audit.log and auditctl -s across restart and concurrent event production; verify strictly non-reused boot sequences, uploaded P1/P2 evidence, degraded recovery and Heartbeat counters."
  - target: Agent audit Collector × audit.log cursor/open serial × incomplete logical event × checkpoint/restart/boot transition × cursor-only versus cursor+pending state
    finding_status: technical_hit
    rating: unrated
    evidence: "A cursor-only checkpoint can advance past SYSCALL/PATH records before EOE and permanently lose that logical event after a crash. AuditdCollector now atomically persists the file device/inode/offset together with bounded pending serial groups, restores same-boot groups, emits old-boot groups as P1 gaps, and retains original raw lines. A crash/restart unit comparison restores one half serial, times it out, enqueues it once with a newly allocated sequence, and rejects linked/shared state files by contract; the broken-symlink case is present but cannot run under the current unprivileged Windows account."
    next: "On Kali kill the Agent after each record of interleaved native serials and around state fsync/replace, then rotate/truncate and reboot; verify every complete operation or explicit gap survives with original lines and no cursor-only loss."
  - target: normalize worker × tenant-wide normalized-event uniqueness × host-local source event × persist/replay × same source ID/content across host variation
    finding_status: technical_hit
    rating: unrated
    evidence: "normalized_events enforces UNIQUE(tenant_id,dedupe_key), but the prior key used source_event_id directly or hashed source/boot/sequence/content without trusted host identity. Two hosts in one tenant could therefore suppress one fact when local source IDs or identical native payloads overlapped; audit boot+serial made the collision concrete. dedupe_key now hashes tenant+host+agent+boot+source scope before the source ID/content digest. Unit comparisons prove same-host replay stability and distinct keys/event IDs for two hosts with identical audit boot+serial or native content. PostgreSQL was not started this turn."
    next: "On Kali PostgreSQL normalize two same-tenant hosts with identical native payload and source_event_id, then replay each; verify two rows remain after first pass and replays keep the row count at two, including concurrent workers."
  - target: Incident worker × Detection/NormalizedEvent/raw evidence × tenant/host/subject relationship × correlate/revise/query × replay, cross-boundary, late, corrupt, and overflow variation
    finding_status: technical_hit
    rating: unrated
    evidence: "P6 had only empty Incident CRUD and no dynamic aggregation/evidence boundary. The new correlator/worker tests prove order-independent replay, separate host/source subjects, fail-closed missing/conflicting evidence, late/time-quality revision flags, raw integrity references, and explicit max+1 overflow. Migration 0008 adds tenant-scoped composite FKs from Incident revisions to detections, normalized events, claims, timeline and edges; API read tests use the authenticated tenant. No real PostgreSQL or dual-tenant HTTP cycle ran this turn."
    next: "On Kali PostgreSQL run tests/integration/test_incident_persistence.py, then use two authenticated tenants/hosts/subjects to query each other's Incident evidence/timeline/claims/graph; verify 404/no rows, exact FK rejection, one concurrent Incident, and append-only late revisions."
  - target: Incident correlator × repeated raw-log context/data reduction × Incident/query reference × aggregate/sample/retrieve × 10k, order, and limit variation
    finding_status: technical_hit
    rating: unrated
    evidence: "An in-memory 10,000-event run deterministically produces one Incident, 20 primary samples, dropped_count=9980, a bounded relationship-edge sample whose IDs all exist in the evidence index, and a full_query_ref with tenant/host/time/type query spec. This closes the contract-level reduction accounting but not PostgreSQL storage, retrieval cost, or concurrent worker behavior."
    next: "On Kali insert 10,000 repeated normalized events and one member detection, run two IncidentWorkers concurrently, verify one Incident/revision and 10000/20/9980 counts, then execute the stored query spec to recover the complete event range."
  - target: authenticated analyst × Incident lifecycle API × current membership/lineage/feedback × merge/split/close/comment × tenant, component, partition, replay, and state variation
    finding_status: technical_hit
    rating: unrated
    evidence: "Mocked lifecycle tests show close resolves current detections and audits, feedback appends a separate row, merge closes only non-target sources and records lineage after one-component recomputation, and split requires an exact detection partition matching deterministic components. These transitions are tenant-scoped in code/schema, but their real transaction rollback, concurrent calls and HTTP authorization were not exercised without PostgreSQL."
    next: "On Kali use two analyst tenants and concurrent requests for merge/split/close/feedback; verify cross-tenant objects remain inaccessible, invalid components return state_conflict without partial rows, successful lineage/audit is append-only, close is idempotent, and worker replay does not reopen resolved evidence."
  - target: authenticated analyst × AI review API/Tool Gateway × Incident revision and normalized evidence × analyze/read/query × tenant, revision, query_ref, tool and request-shape variation
    finding_status: technical_hit
    rating: unrated
    evidence: "P7 binds every package/tool call to server-side tenant, Incident, revision and stored full_query_ref; P8 persistence additionally normalizes program checks, Verifier slots/reviews, conflicts and adjudication under the same task/revision/Claim scope. Unit tests reject undeclared tools, cross-query refs, extra/coerced arguments, unknown Claim evidence and cross-scope P8 records before expanding scope. Migration 0010 and tests/integration/test_ai_review_persistence.py now cover three model roles plus all P8 record types, same-tenant replay and cross-tenant read/Tool denial, but PostgreSQL and dual-credential HTTP were not run this turn."
    next: "On Kali migrate through 0010, use two tenant credentials and two Incidents to POST/GET reviews and call stored-query tools; verify cross-tenant/cross-revision/cross-query attempts return no data, direct FK inserts for every P8 table fail, model history is tenant-isolated, and audit rows retain only the authorized scope."
  - target: untrusted evidence/model × Analyzer prompt and Tool Gateway × instructions/tool arguments/Claims × interpret/execute/report × injection, malformed JSON, duplicate call, evidence and budget variation
    finding_status: technical_hit
    rating: unrated
    evidence: "Focused P7/P8 tests keep injection strings only in untrusted Analyzer/Verifier/Adjudicator data, strip Analyzer identity/scores/verdict/reasoning from BlindClaim, reject malformed or ambiguous output, duplicate/unauthorized tools and cross-evidence Claims, and bound rows/bytes/tool/model/token/cost/rate across all roles. A malicious Claim requesting delete_incident reached only BlindVerifierInput; the write tool was rejected before the data source, and a low-risk whole-log trigger produced zero Provider/Tool calls. This is original local fake-provider evidence, not a real Kimi/GLM/OpenAI-compatible adversarial session."
    next: "Against each configured real Provider, place conflicting instructions in raw logs, Claims, program checks and Tool results, exercise tool-call/JSON/duplicate-parameter variants, and verify no unauthorized tool/data access, nonexistent evidence references remain zero, secrets stay absent from logs, and degradation is auditable."
  - target: high-risk Incident × Analyzer/Verifier/Adjudicator × atomic Claim and assurance × review/compare/adjudicate × provider identity, missing slot, conflict, deterministic assertion, history and budget variation
    finding_status: technical_hit
    rating: unrated
    evidence: "Local dynamic tests always run program verification after Analyzer, validate count/time/hash/host/entity/process/session facts, mark nonexistent evidence invalid, and prove deterministic invalid cannot be promoted by an Adjudicator. Required review with no Verifier is unreviewed+human; same provider/model stays Basic even with perfect history; distinct Analyzer+Verifier reaches Enhanced; two distinct Verifiers with no unresolved conflict reach High; unresolved conflict lowers assurance and requires human. Analyzer+Verifier+Adjudicator consumes exactly the default three-run shared budget. Migration 0010 persists these states, but no real Provider, PostgreSQL, tenant HTTP or calibrated gold dataset was used."
    next: "On Kali replay fixed gold Incidents through at least two real Providers; compare Claims to raw evidence, exercise unavailable and conflicting slots, confirm program-invalid Claims are never overridden, verify high-risk Incidents cannot retain elevated assurance without review, and measure overclaim/miss/conflict rates before accepting routing scores."
  - target: normal Incident/provider outage × AI Review Gate/model client × deterministic detection result × skip/degrade/retry × threshold, timeout, 429, 5xx, circuit and concurrency variation
    finding_status: technical_hit
    rating: unrated
    evidence: "Unit comparisons prove below-threshold normal activity produces zero Provider calls, while timeout/429/5xx retries are bounded, non-retryable/schema failures stop, and circuit/provider failures return model_unavailable with deterministic_result_preserved=true. No live worker+API fault injection or 30 reviews/minute concurrent PostgreSQL run occurred."
    next: "On Kali run normalize/detect/Incident continuously while killing/throttling the real Provider and issuing concurrent reviews; verify deterministic rows and worker latency are unchanged, rate/cost budgets are atomic, one revision/policy produces one task, and recovery closes the circuit without duplicate billing."
  - target: authenticated tenant × raw malware upload API/quarantine × sample bytes/reference × quarantine/read/export × body, filename, tenant, key, ref and ciphertext variation
    finding_status: technical_hit
    rating: unrated
    evidence: "P9 local ASGI and filesystem tests enforce Content-Length plus chunked byte bounds, treat path-like filename only as metadata, encrypt with an independent AES-256-GCM key and AAD-bound tenant/hash/object ID, reject wrong key/tenant/ref/tampering, and return no quarantine_ref. The QuarantineStore protocol and OpenAPI contain no sample get/download/export surface. The HTTP persistence call was mocked and no two-tenant PostgreSQL request comparison ran."
    next: "On Kali PostgreSQL use two tenant credentials to upload and read metadata/tasks, substitute sample/task IDs and X-Tenant-ID, send conflicting length/encoding/chunk shapes, inspect API/log/audit for plaintext/ref leakage, and prove filesystem mount/ACL plus default export denial under real service identities."
  - target: standalone malware worker × encrypted sample/static parsers/scanner adapters × type/family/disposition/context × claim/analyze/persist × malformed archive, unavailable, single, corroborated, hash/size and lease variation
    finding_status: technical_hit
    rating: unrated
    evidence: "Bounded local tests cover script/truncated ELF, ZIP entry-limit-before-parse, compression ratio, traversal/link, raw TAR traversal/link and string limits without extraction or execution. No scanner yields unknown with explicit YARA-X/ClamAV unavailable; one fake hit remains suspicious/family candidate/type unknown; two fake sources corroborate; context cannot name family/type. Worker instrumentation shows transaction depth zero during decrypt/parse and stable integrity error codes. These are fake scanner/Windows results, not real YARA-X/ClamAV or PostgreSQL lease evidence."
    next: "On Kali run blue-team-malware-worker as a separate identity with real version-pinned YARA-X/ClamAV and licensed known/safe samples; fuzz archive/ELF/script metadata, concurrently claim/reclaim/fail tasks in PostgreSQL, verify no API process interprets bytes, and measure targets before setting impact or closing P9."
  - target: sandbox service signer × signed-report import boundary × tenant/sample/hash/observations/artifacts × verify/import × key, signature, scope, schema, size, destruction and replay variation
    finding_status: technical_hit
    rating: unrated
    evidence: "Local Ed25519 verification accepts a correctly bound structured report and rejects wrong sample binding or environment_destroyed=false; Pydantic limits observation/artifact counts and references, and the API applies a raw report byte limit before validation. Migration 0011 has a tenant/sample FK and signature hash audit record. No external sandbox service, live import API, key rotation/revocation, PostgreSQL replay, escape or egress test ran."
    next: "Build the separate virtualization cluster, then on Kali test unknown/revoked keys, valid-signature cross-tenant/sample/hash, duplicate report ID, oversized/duplicate/unknown fields, artifact reference mismatch, report replay, task destruction, escape, credential inheritance and default/controlled egress with original request/state/audit evidence."
  - target: API process/static worker/dynamic sandbox × untrusted sample × interpret/execute trust boundary × deploy/run × enablement and process-role variation
    finding_status: technical_hit
    rating: unrated
    evidence: "A P9 design audit found the first draft could start MalwareWorker in the API lifespan, contrary to the project plan. That startup path was removed before handoff; static interpretation now has only the standalone blue-team-malware-worker entry point, while the repository contains no dynamic sample execution implementation. No deployed process/service-account/network/mount observation exists yet."
    next: "On Kali inspect processes, open files, credentials, mounts and network namespaces while upload/API/static scans run; prove the API never opens quarantine plaintext for analysis, the Agent has no sample access, the static worker cannot execute or export, and any future dynamic sandbox is on a separate virtualization/network/key boundary."
  - target: authenticated analyst × P10 trace build/read/query × current Incident revisions and normalized evidence × correlate/persist/query × tenant, seed, revision, graph bound, replay and concurrency variation
    finding_status: technical_hit
    rating: unrated
    evidence: "Local P10 builder and ASGI comparisons keep the server-authenticated tenant, select only the seed Incident's exact-observable/session connected component, deterministically reproduce the same report under input reordering, reject missing/cross-tenant/overflow inputs, and bound graph root/depth/nodes. Migration 0012 adds append-only trace revisions and composite FKs from source Incidents, trace evidence, edges and techniques back to exact Incident evidence. Repository mocks prove exact replay is a no-op; all generated OpenAPI local references resolve and expose only the bounded typed P10 bodies. Real PostgreSQL, two-credential HTTP, direct FK attacks and concurrent builds were not run."
    next: "On Kali migrate through 0012, use two tenant credentials and same-shaped Incident/trace/entity IDs to build/read/query/export each other's traces, directly insert wrong incident revision/event/edge/technique refs, and race builds; verify 404/FK rejection, one current revision, no partial graph and append-only late revisions."
  - target: P10 trace builder × cross-host session/target activity/observable graph × entry, lateral path, affected scope and infrastructure cluster × infer/report × one-sided, success, NAT/proxy, common infrastructure, time and order variation
    finding_status: technical_hit
    rating: unrated
    evidence: "A synthetic two-host chain reconstructs Web entry, Host A shell, exact SSH session, Host B successful activity, two impacted Hosts and ATT&CK mappings with every step/edge/mapping bound to trace evidence. A two-sided session without target success is restricted to technical communication, unrelated Incidents are excluded, and clusters require exact IP/domain/certificate/file-hash reuse across Host or Incident. This is not a native Kali attack replay and does not yet cover NAT, proxy, jump host, common CDN/DNS or one-sided telemetry at dataset scale."
    next: "Create versioned Kali replay sets for successful/failed Web and SSH entry, one-sided/mismatched sessions, NAT/proxy/jump hosts, shared benign infrastructure, clock skew, reorder, duplicate and late facts; compare reconstructed entry/direction/path/scope to ground truth and retain unknowns where evidence is incomplete."
  - target: untrusted evidence/model/infrastructure metadata × P10 technical and identity attribution × ATT&CK/TTP/real-world actor × map/export × IP, ASN, language, similarity, unsupported evidence and request-shape variation
    finding_status: technical_hit
    rating: unrated
    evidence: "P10 uses a finite versioned rule-to-ATT&CK map with observed/inferred status and exact evidence IDs. Domain, generated Schema and resolved OpenAPI fix IdentityAttribution to status=not_attributed, assertion_count maximum 0 and assertions maxItems 0; a direct identity assertion fails validation. Source/path scan plus ASGI/contract tests found no SQL/URL/file/shell input or identity-writing path from IP/ASN/language/model output. These are local contract/API facts, not an adversarial full-provider or database corruption test."
    next: "On Kali place actor names, geopolitical claims and instructions in raw logs, model Claims, ASN/whois fields, filenames and domains; vary duplicate/unknown JSON fields and database rows; verify identity assertions remain exactly zero and only evidence-backed TTP/similarity survives export."
  - target: authenticated analyst × P10 structured investigation export × trace/evidence pointers/raw refs × export/audit/verify × tenant, revision, hash, content class, replay and size variation
    finding_status: technical_hit
    rating: unrated
    evidence: "Local export tests independently recompute the canonical trace SHA-256, reconcile tenant/trace/revision/evidence count, set raw_content_included=false and sample_content_included=false, and record only manifest metadata in audit. Source/path scan and ASGI output confirm the package carries evidence pointers rather than raw log/sample/quarantine bytes. No real download authorization, retention/deletion, large-package streaming, signature/custody or concurrent export testing ran."
    next: "On Kali request exports with two tenants and stale revisions, tamper manifests/content, exceed practical size, repeat/concurrently export, delete retained evidence and exercise custody signing if enabled; verify authorization, hash/signature detection, bounded resources, retention policy and audit completeness before closing P10."
  - target: mTLS client × locally issued server leaf × server identity × handshake × strict OpenSSL certificate-chain variation
    finding_status: closed
    rating: unrated
    evidence: "Python 3.14/OpenSSL strict verification dynamically rejected the authenticated client connection with verify_code=85 'Missing Authority Key Identifier'. LocalCertificateAuthority.issue_server_certificate omitted SKI/AKI while agent leaves included them. The server leaf now includes SubjectKeyIdentifier and AuthorityKeyIdentifier; tests/unit/test_agent_identity.py::test_real_tls_requires_a_ca_signed_client_certificate passes with a CA-signed client and still rejects a client without a certificate."
    next: "Reopen if Kali/OpenSSL, an external CA, certificate rotation, hostname/SAN variation, CRL/OCSP, or the real ingest TLS listener rejects the chain or accepts an unauthenticated peer."
  - target: operator browser × console same-origin proxy × tenant Snapshot × read × token, target URL, redirect, size and transport variation
    finding_status: lead
    rating: unrated
    evidence: "The console stores the operator token only in React memory and forwards it to one fixed /api/v1/console/snapshot path. Route source and rendered production tests verify no browser storage/cookie use, no caller-supplied target URL/body, redirect=manual, 1 MiB response limit, timeout, JSON-only response, and HTTPS for non-loopback origins. No live API, second tenant, expired token, redirecting upstream, oversized/slow server or browser network capture ran this turn."
    next: "On Kali connect the built console to the real HTTPS API using valid, expired, revoked, wrong-role and cross-tenant credentials; exercise redirect, oversized/chunked/non-JSON/slow upstreams and inspect browser/server logs for token or data leakage."
  - target: authenticated operator × fixed console Incident routes × current Incident revision/evidence membership × investigate/read normalized fact × tenant, Incident ID, evidence ID, revision, truncation and raw_ref variation
    finding_status: lead
    rating: unrated
    evidence: "The new console investigation repository takes a PostgreSQL read lock on the tenant-scoped current Incident row before bounded section queries; contract/API tests require exact production inc_/evi_ IDs and responder/approver/auditor roles. Repository tests compile tenant+Incident predicates, 100 evidence, 200 timeline/Claim/entity and 400 visible-edge limits, and a separate evidence request rechecks tenant+Incident+revision+evidence_id before joining the normalized event by event_id. Production worker requests dynamically reach only the two fixed console Incident paths and reject path-like IDs before upstream fetch. The UI uses React text/JSON rendering with no dangerouslySetInnerHTML and displays truncation/query refs; it does not dereference raw_ref or fetch object-store bytes. Database rows/upstreams were mocked and no second tenant, concurrent revision, real PostgreSQL lock, oversized normalized payload, browser capture or raw evidence authorization ran."
    next: "On Kali/PostgreSQL use two tenants and concurrent Incident revision writers while responder/approver/auditor read details; substitute Incident/evidence/event/raw_ref IDs, test limit+1 datasets and 1 MiB proxy bounds, corrupt link rows under controlled fault injection, and verify the browser/API never resolves arbitrary raw_ref. Decide whether audited short-lived raw-object access is required before adding it."
  - target: authenticated operator × fixed console malware route × sample/report/same-hash context × investigate/read × tenant, sample, hash, task, engine, context, size and untrusted URL variation
    finding_status: lead
    rating: unrated
    evidence: "The console malware repository holds a read lock on the tenant-scoped sample, independently revalidates the latest report's tenant/sample/task/hash/size and normalized engine index, and joins same-hash contexts only through tenant-bound sample rows. Contract/repository tests enforce 50 task, 8 context, 8 engine, four per-engine value and four per-context evidence limits with original counts/truncation flags; serialized output omits quarantine_ref, sample bytes, static strings and archive entries. The production worker reaches only /api/v1/console/malware/smp_[hex32], rejects path-like IDs before upstream fetch, and React renders source_url as text rather than a link. Rows/upstreams were mocked; no real PostgreSQL lock, second tenant, concurrent scan, corrupted report/index, maximal escaped payload or browser network capture ran."
    next: "On Kali/PostgreSQL use two tenants and concurrent scan completion/rescan while responder/approver/auditor read details; substitute sample/hash/task/engine/context/host IDs, create limit+1 and escape-heavy reports under the 1 MiB proxy boundary, corrupt report/index relationships under controlled fault injection, and verify source_url/quarantine_ref can neither trigger navigation nor retrieve sample/object bytes."
  - target: operator browser × fixed console response routes × tenant response action × detail/approve/reject/queue/rollback × origin, credential, ID, body, nonce and idempotency variation
    finding_status: lead
    rating: unrated
    evidence: "Production worker requests dynamically reject missing Origin/Referer, malformed rsa_ IDs, nonce reuse with another Bearer, redirects and declared oversized responses before an upstream effect. Valid requests reach only fixed detail/approvals/execute/rollback paths with field-minimized JSON; write-session HMAC binds exact origin, Bearer, issue time and random nonce, while token/nonce remain React-memory only. ASGI tests dynamically enforce auditor/responder/approver role separation, typed action IDs, execution-disabled fail closure and authenticated tenant/actor forwarding. Upstreams and repositories were mocked; no real browser, HTTPS API, PostgreSQL state/audit comparison, second tenant or native response action ran."
    next: "On Kali configure an independent console CSRF secret and real HTTPS origins, then compare requester/responder/two approvers/auditor across two tenants and two actions; vary missing/cross Origin/Referer, expired/tampered/wrong-token nonce, duplicate/unknown/oversized JSON, ID substitution, repeated/concurrent idempotency keys, disabled runner and stale states, and reconcile action/events/audit/outbox with UI results."
  - target: approved responder × response worker/Adapter × exact remote target × execute/rollback × lease, target identity, retry and Linux state variation
    finding_status: lead
    rating: unrated
    evidence: "P11 now has an independent blue-team-response-worker and initial nftables/firewalld block, regular-file quarantine and exact-allowlist account adapters, but only under an explicit local_single_node profile loaded from the private local Agent config. Stateful fake backends prove three execute/post-check/rollback flows, remote tenant/host/agent mismatch before commands, root/account denial, bounded argv execution, and verification_failed classification after attempted writes or result-commit failure. Native execution remains disabled in .env.example; no nft/usermod/file mutation ran on Kali, no real write or rollback capability has been demonstrated, and multi-host Agent-side dispatch does not exist."
    next: "On an isolated Kali single-node deployment validate all three reversible actions with two approvers where required, target substitution/PID-inode/path/account reuse, command timeout/output, result commit loss, lease expiry, TTL, repeated rollback and audit; separately design authenticated Agent-side dispatch before enabling any multi-host action."
  - target: response outbox × standalone notification worker × fixed Webhook destination × deliver/retry/DLQ × lease, URL, redirect, response, signature and payload variation
    finding_status: lead
    rating: unrated
    evidence: "Local unit requests prove canonical HMAC-SHA256 envelopes, stable idempotency IDs, topic-specific field minimization, exact configured host allowlisting, HTTPS outside loopback, userinfo/query/fragment/special-address rejection, no redirect following, timeout/status mapping and a 64 KiB response bound. Repository/worker instrumentation proves digest-only leases, HTTP at transaction depth 0, stale-lease recovery before replay, bounded exponential retry, terminal DLQ and append-only attempt metadata. Migration 0014 and a PostgreSQL integration test were added, but no real database, DNS/TLS peer, concurrent worker or external receiver ran this turn."
    next: "On Kali migrate through 0014, run two notification workers against a controlled HTTPS receiver, then vary concurrent claims, DNS/certificate, timeout, 429/5xx/redirect/oversized responses, key rotation/replay and cross-tenant payloads; verify one logical effect per event, valid signatures, bounded retries, DLQ observability and no extra sensitive fields."
  - target: authenticated operator × fixed console rule/intelligence route × rule versions/hits/current-Incident feedback/enrichment cache × read/govern × tenant, role, version, lifecycle, payload, indicator and limit variation
    finding_status: lead
    rating: unrated
    evidence: "A version-bound catalog covers all nine runtime rules and local tests fail closed on missing entries or rule version/applicable-event-source drift. Migration 0016 adds tenant current state, append-only lifecycle events, shadow observations, governance references on detections, and version-aware dedupe. Ed25519 tests bind key/tenant/rule/version/catalog/exact dataset set/time, while repository tests cover continuous sequence+previous hash, Draft→Shadow→Canary→Released, stepwise rollback, deprecate, upgrade, replay, expired/version-stale/catalog/evidence-corrupt fail closure, and bounded tenant Canary Hosts. DetectionWorker dynamically emits only current Canary/Released scope, writes Shadow/non-Canary matches outside detections, and emits nothing for missing/Deprecated state. Tenant-admin import and auditor/admin read ASGI requests reject missing trust stores, wrong roles, and verification errors. The console now reports lifecycle_enforcement_available=true with effective scope, sequence, manifest/key/catalog, Canary/validation bounds and governed/legacy/shadow counts; managed_ioc_lifecycle_available remains false and no unsigned control exists. The full non-Docker gate is Ruff clean across 260 files, mypy strict across 258 source files, Schema clean, 529 passed/22 skipped, Alembic 0016 upgrade+downgrade offline SQL, console production build+6 tests, and zero known pip/npm vulnerabilities. Lifecycle rows, locks, FKs, concurrency, dual-tenant HTTP and browser evidence remain mocked/skipped without PostgreSQL/Kali."
    next: "On Kali/PostgreSQL migrate through 0016 and run test_rule_lifecycle_persistence.py plus the full detection/Incident pipeline with two tenants, two admins/auditors and same-shaped Host/rule IDs. Race first import/replay/rollback, substitute cross-tenant Canary Hosts and manifests, expire or corrupt state under controlled fault injection, and verify Shadow never reaches detections/Incidents while Canary/Released rows retain exact manifest references. Separately test rule hit/history/current-revision feedback/cache isolation, limit+1/HTML indicators and browser/network disclosure; design managed IOC ownership/expiry/disable/audit separately from cache visibility."
  - target: authenticated operator × fixed console model-operations route × Provider config/review tasks/model runs × read/assess × tenant, role, key state, health, quality, status and limit variation
    finding_status: lead
    rating: unrated
    evidence: "Local contracts/repository/API/worker tests prove tenant-scoped review/run aggregates, bounded groups/recent rows, fixed proxy routing, secret/base-URL/Prompt/response exclusion, and explicit not_tested/not_probed/no-labeled-linkage states. Database and upstream rows were mocked; no PostgreSQL, two-tenant credential, live Provider, key validation, browser capture, or ground-truth performance evidence exists."
    next: "On Kali/PostgreSQL compare two tenants and roles using same Provider/model/run shapes; inject unknown statuses and limit+1 groups, validate key/base URL/log/browser non-disclosure, then test live Provider timeout/circuit/recovery and separately design audited credential health and labeled feedback linkage before publishing model quality."
  - target: auditor/tenant_admin × fixed console system-operations route × tenant work states/Agent queue/storage records/errors/runtime and migration versions/current-binding Agent versions/credentials × read/operate × tenant, role, status, queue/version shape, limit and telemetry-availability variation
    finding_status: lead
    rating: unrated
    evidence: "Local contract/repository/API/worker tests prove auditor/admin-only tenant-scoped work-state and credential projections, status reconciliation, bounded 100 credentials/1000 latest heartbeat Hosts, QueueTelemetry validation, current Host-Agent binding version aggregates with at most 50 reconciled groups, source=self_reported_heartbeat and binary_integrity_verified=false, fixed proxy routing, token/digest/error-detail exclusion, and explicit unavailable broker/capacity/dependency/deployment/upgrade/backup states. The full non-Docker gate is Ruff clean, mypy strict 249 files, Schema check, offline migration through 0015, 483 passed/21 skipped, console production build plus 6 tests, and zero known pip/production npm vulnerabilities. Database/upstream rows were mocked; no PostgreSQL, two-tenant credentials, real Agent heartbeat fleet, browser capture, broker/storage probe, signed release comparison, upgrade or backup evidence exists."
    next: "On Kali/PostgreSQL compare auditor/admin/responder/approver identities across two tenants with same credential/status/queue/version shapes; inject unknown statuses/roles/malformed queue telemetry, Agent rebind/version replay and limit+1 rows, inspect browser/logs for token/digest/error-detail leakage, verify observed alembic version, then separately implement and fault-inject broker backlog age, capacity, dependency health, signed rollout/rollback and backup/restore evidence before claiming operational readiness."
  - target: mTLS Agent × backward-compatible heartbeat/current Host binding × self-reported Agent version × report/persist/inventory × tenant, Agent, Host rebind, version omission/malformed value and latest-heartbeat variation
    finding_status: lead
    rating: unrated
    evidence: "Local contract/runtime/transport/repository tests show new runtimes emit bounded semver, legacy heartbeats may omit it, malformed versions fail validation, ingest persistence is certificate-identity-bound in code, and console SQL selects only the current Host-Agent binding with at most 50 reconciled version groups. Generated schemas make inventory availability true while binary-integrity, deployment, rollout and signed-artifact capabilities remain false. The full non-Docker gate is Ruff clean, mypy strict 249 files, Schema check, offline migration through 0015, 483 passed/21 skipped, console production build plus 6 tests, and zero known pip/production npm vulnerabilities. The version value remains self-reported; PostgreSQL mTLS integration is skipped and no real two-Agent rebind, signed release state, browser capture or fleet exists."
    next: "On Kali/PostgreSQL enroll Agent A, report a version, rebind/re-enroll Agent B on the same-shaped Host flow, vary omitted/malformed/replayed versions and concurrent heartbeats, and verify only the current binding appears. Compare reported version to signed installer state and running binary before enabling integrity/compliance claims."
  - target: authenticated operator × fixed console attack-trace route × current seed Incident trace revision × investigate/read × tenant, Incident, revision, hash, truncation, evidence, graph, attribution and query variation
    finding_status: lead
    rating: unrated
    evidence: "Local repository/API/Schema/production-worker tests bind responder/approver/auditor reads to the authenticated tenant and one exact inc_ seed route. The repository query contains tenant+seed predicates and PostgreSQL FOR SHARE, then reuses P10 current revision/report scope/canonical snapshot hash validation; a corrupt hash fails closed. The browser projection bounds source Incidents/evidence/key path/impacted Hosts/clusters/techniques/entities/edges at 50/100/100/100/50/50/200/400 with eight-item reference samples, reconciles truncation, retains conclusion-evidence and graph-endpoint closure, and omits raw_ref, raw bytes and entity attributes. Identity assertions remain 0; arbitrary graph query/export flags are false and the proxy rejects malformed or extra query/path inputs. The full non-Docker gate is Ruff clean, mypy strict 249 files, Schema check, offline migration through 0015, 483 passed/21 skipped, console production build plus 6 tests, and zero known pip/production npm vulnerabilities. These remain local Mock/ASGI/contract/build facts; real PostgreSQL locks, two tenants, concurrent revisions, a deployed browser and limit+1 datasets were not exercised."
    next: "On Kali/PostgreSQL use responder/approver/auditor and wrong-role credentials across two tenants with same-shaped Incident/trace/entity IDs; race a current trace revision while reading, inject missing/corrupt revision/hash and dangling evidence/edges under controlled fault injection, exercise every section at max+1 plus the 1 MiB proxy bound, and inspect browser/network/logs for raw_ref, attributes, identity or token leakage. Keep interactive graph query/export unavailable until separately authorized, bounded and audited."
```
