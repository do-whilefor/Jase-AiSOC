# P0 安全不变量静态评审记录

评审范围：`aisoc-contracts`、JSON Schema 导出清单、Web/SOC/Control/Response Contract Test 源文件，以及七平面架构和威胁模型。

评审方式：在 Windows 无 Rust 环境下进行源码和文档静态审阅。未执行编译、测试、Schema 导出、服务运行、数据库或故障注入。本记录只证明安全不变量已经形成可审查的 Rust 契约和负向样本，不替代计划书要求的 Linux 动态验证。

## 评审矩阵

| 安全不变量 | Rust 权威守卫 | 负向样本 | 静态评审结论 | 待 Linux 证据 |
|---|---|---|---|---|
| 鉴权上下文决定 tenant，客户端字段不得覆盖 | `validate_agent_binding`、`validate_web_binding`、`validate_authentication_context`、`validate_tenant_scope`、`AuthoritativeObjectScope` | Agent context/envelope 版本、envelope tenant/agent/host、嵌套 Event tenant/host/agent/boot/sequence、payload 空/超限、Event/Evidence 子契约、canonical digest 替换；Web ingress context/request 版本及 tenant/service/route 替换；Control context version、缺失/双 actor、OIDC/mTLS actor kind、roles/attributes、声明 tenant/对象替换、服务端解析 owner tenant 不一致 | AgentBindingDecision 24/24、WebBindingDecision 6/6、AuthenticationContextDecision 8/8 和 TenantScopeDecision 6/6 结果均具备直接样例；mTLS Agent context 对 envelope 和每个 Event 的 tenant/agent/host 具权威性，Web context 对 request 的 tenant/service/route 具权威性，boot/sequence/typed 子契约/digest 继续闭合；Control context 恰一 actor 并绑定 auth kind，鉴权 tenant、客户端声明与 repository/registry 解析出的真实对象 owner 三方独立；对象引用必须一致；fail-closed decision 明确 | 双租户/双身份 Contract Test；mTLS/OIDC 实际鉴权上下文；证书吊销与重放；repository-backed owner lookup |
| Schema、ID 和枚举 fail closed | `validate_current_schema`、`deny_unknown_fields`、typed ID/registry ID/enum | 未知 Web 字段、未知 Policy decision、v2 输入、URL-shaped StoreId/WafRuleId、含冒号/空分段/traversal-shaped WafRuleId | SchemaVersionDecision 2/2 结果具备直接样例；当前 major 只接受 `1.0.0`；Agent/Web/Control/Evidence auth context 同样版本化；所有 `_id`/`_ids` 跨边界字段均为 newtype；WAF selector 的 Rust 反序列化与 Schema 均只允许安全非空 `/` 分段；未知字段不进入 DTO | 生成 Schema 并执行 drift=0；兼容矩阵 |
| Raw Evidence append-only 且确认结论需要完整证据 | `validate_security_event`、`validate_detection_contract`、`EvidenceRef`、`validate_evidence_access_context`、`authorize_evidence_use`、`validate_incident_relationships`、`validate_incident_revision_transition`、`validate_evidence_package_binding`、`verify_claim_evidence`、Incident/Detection confirmed 守卫与 Web 状态上限 | SecurityEvent Schema/Evidence tenant/子契约、entity/source/Linux 子对象/metadata 容量；Detection Schema/Evidence tenant/子契约/去重/必需性、confirmed size/integrity/custody、window/count、Rule/entity/suppression；访问上下文版本不支持/重复/超限、跨租户、非 Incident member、classification 越级、子契约无效、重复引用、零字节、pending/failed integrity、custody expired、revision 后 Detection/Claim/Evidence、Incident 集合缺口、Detection Evidence/Entity 缺口、同 Evidence ID 身份替换、Evidence 生命周期回退、访问上下文混入 revision 外 Evidence、confirmed 缺少任一 Evidence 访问权、Claim Evidence/Detection-origin 缺口、无 confirmed 支持却自报 confirmed、非相邻 revision、revision tenant/incident/time 替换、关系删除、Timeline 改写或重复事实删减、package revision/成员/metadata 替换、Claim 后采集 Evidence、非 verified Claim 引用坏 Evidence、无 Evidence、Assurance 未验证、Web 直接声明 confirmed | SecurityEventDecision 20/20 与 DetectionContractDecision 17/17 结果均有直接样例，源事件到 Detection 的 Evidence tenant/子契约/集合及确认状态边界 fail closed；Evidence 二次授权按版本化上下文、Evidence 子契约、tenant、Incident membership、classification、size、integrity 和 custody fail closed；Evidence 失败、零字节或 custody expired 仍可保留历史引用/审计，但不可支持 confirmed Claim/Detection/Incident；Incident 与服务端解析的 Detection/Claim 集合精确闭合，Detection Evidence/Entity 和 Claim Evidence/origin 必须纳入该 revision；访问上下文只能引用 revision 成员，普通 observed revision 可按权限使用 Evidence 子集，而 confirmed revision 必须验证全部 Evidence 可访问；Evidence 的 integrity/custody 允许随生命周期演进，但 locator/hash/size/source/classification/time 等不可变身份不得替换且生命周期不得回退；相邻 revision 绑定 tenant/incident/time，保留既有 Detection/Evidence/Claim 与 Timeline 事实多重集，late event 可按 occurred_at 插入；confirmed Incident 至少由 confirmed Detection 或独立验证的 confirmed Claim 支持；EvidencePackage 必须绑定权威 Incident ID/revision；任何 status 携带的 Claim Evidence 引用都先验证；Web 不能直接产生成功/失陷结论 | Repository-backed object resolution、Hash、custody transition、classification、权限动态验证 |
| 模型输出不是事实或执行动作 | `Claim`、`ModelAssessment`、`EvidencePackage`、`validate_model_assessment_binding`，不依赖 Response executor | 重复 Claim/Evidence、超预算 EvidencePackage、包创建后采集 Evidence、跨 tenant package、其他 model run Claim、未返回 Claim、包外 Evidence、完成后 Claim、伪造 Evidence、verifier 元数据只出现一半、无 verifier 的 Verified Claim、proposed/verified readonly tool 自我验证、只有合法 Evidence 的 proposed Claim、跨租户 contradicted Claim、human-review 状态 | AI 输出只引用 Claim/Evidence；无 Action 字段和执行能力；ModelAssessment 使用 typed Provider/Model/Prompt ID 并携带版本；EvidencePackage 最多 512 项/64 MiB，且不能包含晚于包创建时间才采集的 Evidence；Incident assessment/package/claims 绑定为同 tenant/incident/model run、精确 Claim 集合、包内 Evidence 和 package→claim→completion 时间闭包；所有 Claim 状态先绑定访问上下文 tenant/incident，再解释 contradicted/unsupported/human-review；verifier ID/version 成对，tool identity 在任何 Claim 状态下都不得同时充当独立 verifier；proposed + 合法 Evidence 只返回 EvidenceValidated，不能返回 Verified | Provider mock/真实故障、Strict JSON、可计算断言 verifier、tool gateway 租户矩阵 |
| Web 攻击者输入是数据且敏感字段最小化 | `validate_web_request_contract`、`validate_web_data_minimization`、`validate_web_security_event` | Authorization/password/token/secret 字段、非法 method token、authority/URI/parser 空值/歧义/超限、selected name/value/count/sample 超限、monitor/shadow 伪报阻断、非法/重复 Rule hit/reason、非法 HTTP status、Evidence 跨 tenant/子契约失败/重复/空/超限/晚于 decision、Web 直接声明 confirmed | WebDataMinimizationDecision 4/4、WebRequestContractDecision 22/22 和 WebSecurityEventDecision 21/21 结果均有直接样例；扩展 HTTP method 允许，authority/URI 歧义字符 fail closed，selected field/value 有界；敏感名拒绝；monitor/shadow 无执行型 decision；顶层结果集合与文本有界；攻击/阻断事件必须保留同 tenant、契约有效、非空且时间有效的 Evidence；Web 只表达 observed/attempt/blocked，成功/失陷必须由后续 Evidence 链判定 | H1/H2 framing、smuggling differential、Unicode/percent/body fuzz、模式切换回归 |
| AI 不可用不影响确定性主链，Web 失败按 route 明确策略 | `WebRouteFailPolicy`、`WebDecisionBasis`、`validate_web_fail_policy_application` | 无效 event/policy、非 route-fail 来源、AI failure 决策与 route policy 不一致、跨 tenant/service/route、Policy ID/version 替换、decision source 缺失/混用或夹带 deterministic hit、invalid-output 缺少 model run、模型/失败策略伪报 CHALLENGE/RATE_LIMIT、非法 Policy version | WebRouteFailPolicyDecision 3/3 与 WebFailPolicyApplicationDecision 9/9 结果均有直接样例；budget exhausted/timeout/circuit open/unavailable/invalid output 均有显式 disposition；WebSecurityEvent 必须声明完全互斥来源，invalid output 必须关联失败的 model run；CHALLENGE/RATE_LIMIT 只接受 deterministic source；route fail policy 由服务端加载并绑定 tenant/service/route/Policy ID/version，客户端事件不能选择自己的 fallback | Provider timeout/circuit/invalid-output 故障注入；route policy 热更新、回滚和跨 route 矩阵；验证 SOC 确定性事件在 AI 全不可用时继续流转 |
| Response 只接受固定动作并重验证目标 | `ResponseActionType`、`ResponseCapability`、`TargetSnapshot`、`compute_response_action_digest`、`validate_response_contract` | schema/tier/action/capability/target/Incident 不匹配、target 稳定参数无效、目标变更后摘要失效、Linux 路径穿越、空/重复/超限 Supporting Evidence、无效有效窗/idempotency、R0 approval、审批摘要/reject/count/ID/approver/time、低风险标签配合 R3 无审批、R2 TTL/registered inverse/deadline、R3 human runbook、URL-shaped recovery ref | 35/35 ResponseContractDecision 结果具有直接样例；无 Shell/SQL/URL；写动作绑定固定 tier/capability/target；canonical digest 由固定字段重算；target 参数有界并绑定 Incident；有效窗最多 24 小时、approval/approver 唯一且最多 16 项、高风险 Evidence 非空唯一且最多 512 项；审批门槛由 action→tier 决定，低风险标签不降低 R3 门槛；R2 TTL/registered inverse；R3 人工审批与 opaque runbook；关键资产双审 | Runner capability/target revalidation、repository-backed Evidence、approval replay/idempotency、真实 rollback/post-check |
| Secret 不进入日志/错误/UI | `validate_safe_fields`、Audit/Error 守卫、`ErrorCode::public_message`、`ErrorCode::may_retry` | `access_token`、`database_password`、含空格的字段键、控制字符值、带内部路径/密码的任意错误消息、authorization denied 标为 retryable | SafeFieldsDecision 8/8 与 ErrorContractDecision 6/6 结果具备直接样例；审计/错误安全字段有数量、长度、token 键、控制字符和敏感键拒绝；API message 必须等于 ErrorCode 规范公开文本；仅冻结的暂态错误可声明 retryable | 日志/响应/浏览器 bundle Secret 扫描；调用端 backoff 行为 |
| 审计链可关联且关键对象 typed | `AuditEvent`、`AuditCorrelation`、`AuditObjectRef`、`compute_audit_event_hash` | 含 Secret/换行的审计属性、代码/版本字段 CRLF、主体歧义、tenant object 替换、Model/Prompt/Schema/Rule release/Policy/ResponseAction object-correlation 不一致、哈希后字段/前链替换 | AuditContractDecision 15/15 结果具备直接样例；actor 单一；tenant 必填；必填 `AuditObjectRef` 是主审计引用，Model/Prompt/Contract Schema/Rule release/Policy/ResponseAction 使用 typed ID 与版本/release 引用；optional correlation 只作辅助索引，不强制重复主对象字段，但同类型 object/correlation 不得矛盾；当前事件摘要绑定所有冻结内容及 `previous_event_hash`，不匹配即拒绝 | Append-only journal、并发、首事件规则、跨分片锚点、断链和重放检查 |

时间关系守卫统一调用 `Timestamp::is_before` / `Timestamp::is_after`，使用 RFC3339 解析后的时间值比较；`Timestamp` 不实现 `Ord`，避免业务代码误用字符串字典序比较带不同时区偏移的时间。EvidencePackage 与 Claim 的 Evidence 时间闭包均允许同一时刻，只拒绝严格晚于容器/断言创建时间的采集记录。

## 静态评审结论

P0 的七项核心安全不变量已形成 Rust 权威契约、显式 decision enum 和负向测试源文件。Audit/Error 及其 SchemaVersion/SafeFields 公共前置决策已经闭合直接结果样例；关键跨边界集合、文本、扩展对象与有效时间窗均已补充显式上限。未发现 Python/TypeScript 生产运行依赖、Windows 专属实现、任意 Shell/SQL/URL 执行器或关键 ID 退化为普通字符串。

P0 仍不能关闭：Schema 快照尚未由 Linux 锁定 toolchain 生成，四域 Contract Test 尚未执行，动态身份/对象/状态/解析/重放/故障变量尚未验证。正式阶段状态保持“代码已实现，待 Linux 编译和验证”。
