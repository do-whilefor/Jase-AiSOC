AI-SOC V4 sandbox validation summary — 2026-08-11

PASS
- Rust-first production gate, including exact 18 V4 native default workspace members.
- make deploy-check.
- SQLx migration gate: 5 migrations / 15 tables.
- Central PostgreSQL repository gate.
- release manager package/activate/upgrade/rollback/signature tests.
- V4 Rust contract schema check: 23 authoritative DTOs.
- 9/9 migration-period Python differential replay datasets.
- canonical replay SHA + SecurityEvent JSON Schema: 589 events / 9 datasets.
- Python compileall for src/scripts/tests/migrations.
- Rust lexical delimiter auxiliary check: 55 files (not a compiler substitute).

BLOCKED / FAIL
- cargo fmt --all --check: blocked, cargo unavailable, rc=127.
- cargo clippy --locked --workspace --all-targets --all-features -- -D warnings: blocked, rc=127.
- cargo test --locked --workspace: blocked, rc=127.
- cargo build --locked --workspace --all-targets: blocked, rc=127.
- cargo test --locked -p aisoc-detection --test replay: blocked, rc=127.
- real Rust service startup/health test: blocked; no compiled release binaries and no Cargo toolchain in sandbox.
- Cargo.lock gate: FAIL; lock file is stale and lacks 17 native workspace packages plus pinned workspace dependencies.
- Python full pytest: collection blocked (15 errors, 1 skipped) by sandbox dependency mismatch/missing structlog, aiohttp API mismatch, and missing yara_x.

Do not treat BLOCKED commands as passing. First release blocker is to regenerate Cargo.lock with Rust 1.82 in a network/cache-enabled Rust environment, then run the complete Rust gate and fix compiler/test findings.
